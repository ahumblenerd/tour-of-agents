# tinyagents.dev status — 2026-07-05

Snapshot at end of a heavy shipping day. Arun is stepping away from this machine for ~1 month. This doc is the entry point for when he (or a future Claude session) picks it back up.

## The one-page picture

- **Site status:** autopilot mode is compounding. GSC +130% clicks, +135% impressions vs prior 28d. GA4 users +33%. Organic Search fully replaced push traffic.
- **Product:** the funnel top is the leak. 688 visits → 81 lesson_started (-88%) → 24 code_executed L2+ → 3 course_completed / 28d. Everyone who runs code on L2+ completes at least one lesson.
- **Consulting side:** banner live sitewide, 0 bookings so far. Full attribution wired (PostHog + GA4).
- **Two uncomfortable reps not done:** watch 5 session recordings, DM the 3 course completers.

## What shipped 2026-07-05 (12 commits)

| Commit | What |
|---|---|
| `afc06d8` | De-slop /vs/* corpus + jump nav + TL;DR verdict component |
| `77743ef` | Refresh stale framework facts (Mastra Apache/Series A, BabyAGI archived, LangGraph 1.0 GA, Pydantic AI v2, Google ADK 2.0 + Java) |
| `15c7173` | Add Eve (Vercel) + Flue (Astro/Cloudflare) with 6 hand-authored /vs pairs |
| `d125775` | Sitewide consulting banner (cal.com/0xahd/30min) |
| `b8622a1` | Noindex 4 dead /compare/[framework] pages |
| `abbbf82` | Exp 1: mid-page course pitch on all 325 /vs/* pages |
| `ce1381f` | Exp 2: "Try Lesson 1 →" primary CTA in the site header |
| `1d4b223` | Exp 3: track next-lesson clicks by source (toast / prominent / nav) |
| `b6fd960` | UTM tags on Cal.com URL: utm_source=tinyagents + utm_medium=banner + utm_content=<pathname> |
| `e7d7c7e` | README refresh (26 frameworks, 325 /vs pages, "marketing in repo by choice" section) |

Also configured (not in git):
- PostHog app installed on Cal.com "30 min meeting" event type — fires PostHog events on cal.com booking pages using project key `phc_F292o9WNGa3vNrT3dA8E1u4RnIrIlxstT93u5y2HRzR`
- GA4 app installed on Cal.com same event, measurement ID `G-1LWH9HJ17W`
- GA4 key events: `click_book_a_call` (top of funnel, filters `click` events where `link_url contains cal.com`) and `booking_confirmed` (bottom of funnel, filters `page_view` where `page_location contains isSuccessBookingPage=true`)

## Where to look for signals when you return

### PostHog (project 336450)

Custom events to check:
- `consulting_banner_clicked` — did the sitewide banner earn clicks? (pathname is on the event)
- `vs_course_pitch_clicked` — grouped by `position`: `inline` vs `bottom`. Which placement wins?
- `header_try_lesson_clicked` — did the header CTA pull weight? Compare to `vs_course_pitch_clicked`.
- `next_lesson_clicked` — broken down by `source` (toast / prominent / nav). Whichever wins is the one to keep.
- Live events tab — verify cal.com pageviews are still flowing from the PostHog app on Cal.

Funnel to re-run (last 28d, ordered, 14-day window, test accounts filtered):
```
$pageview → lesson_started → code_executed (lesson > 1) → lesson_completed → course_completed
```
Compare against the baseline: 688 → 81 → 24 → 24 → 3. If lesson_started climbs above 110 in 28d, Exp 1 or 2 is working.

### GA4 (property 531202851)

- **Realtime tab first** to sanity-check both key events are firing
- **Reports → Engagement → Events** — filter for `click_book_a_call` and `booking_confirmed` and read the counts
- **Advertising → Attribution** — once bookings arrive, see which source/campaign converted
- Suggested audiences to create when you have volume (deferred from today): see the paste-ready spec in the older chat log — most of them need GA4 events forwarded from PostHog, which is not shipped yet

### Cal.com

- **Bookings** tab — any real bookings? Each one has UTM metadata attached (source=tinyagents, content=<pathname>)
- Attribution comes from PostHog + GA4, not Cal directly — Cal's insights page is basic

### Firecrawl / uptime

- No firecrawl monitors configured (checked earlier — none exist, none draining credit)
- Site health: 200 OK, SSR intact as of 2026-07-04, Railway serving via `railway-hikari`
- Sitemap has 235 URLs (up from 36 in March)

## Decision matrix — do this when you return

Read this after checking the signals above.

### If GSC clicks are still growing (>140/28d)
Autopilot is winning. Keep hands off. Continue depth-update automation, no new work.

### If GSC clicks flatlined or declined
Look at which /vs/* pages lost impressions. Options: (a) refresh their `lastDepthUpdate` field to signal freshness, (b) hand-rewrite the top 3 for quality, (c) check if new frameworks launched that need adding.

### If any experiment shows clear movement
- `vs_course_pitch_clicked` (inline > bottom by any margin) → apply the inline-pitch pattern to /blog/* and /compare/* index pages
- `header_try_lesson_clicked` > 20 in 28d → keep. If <5, delete the header CTA (visual clutter).
- `next_lesson_clicked` clearly dominated by one source → trim the other two. E.g. if `source=prominent` accounts for 80% of clicks, remove the toast + nav variants.

### If any Cal bookings arrived
Whatever you decide about consulting, do it now while there's momentum. See `.agents/attribution-setup.md` (write this if it doesn't exist yet) for the full attribution architecture.

### If nothing moved
The site is a portfolio piece + brand engine per [[project_tinyagents_hands_off_2026_07_04]]. Two options:
1. Roll back the 3 experiments (they added noise for no lift) — 15-min task, revert commits abbbf82, ce1381f, 1d4b223
2. Do the two uncomfortable reps (recordings + DMs) that you skipped today
3. Reallocate hours to Origm AI

## The two uncomfortable reps still not done

Per [[feedback_nudge_hard_things]] and [[user_founder_assessment]] — the two things you've been avoiding all session:

1. **Watch 5 PostHog session recordings.** Filter to /vs/* pages, duration 30-90s. This is worth more than every dashboard combined at 688 users/month.
2. **DM the 3 course completers.** Find them in PostHog person profiles → LinkedIn/Twitter → *"You finished A Tour of Agents. What did you build after?"* Zero-pitch, pure curiosity.

If you do only two things when you return, do these.

## Files worth re-reading

- [`cmo-tracker.md`](cmo-tracker.md) — running log of shipped marketing work
- [`seo-audit.md`](seo-audit.md) — technical SEO baseline
- [`../CLAUDE.md`](../CLAUDE.md) — dev loop + CSS + test rules that any future session needs to follow

## What deliberately isn't shipped yet (options for later)

- Wire GA4 event forwarding in `posthog.ts` (~20 lines) — unlocks proper GA4 audiences based on real events
- GTM container migration for full Cal + GA4 attribution (only worth it once bookings become recurring)
- Rewrite the 2 dead blog posts (`ai-agent-framework-ranking-2026` 1s dwell, `langchain-alternatives` 6s dwell) or noindex them like we did the /compare pages
- Hand-rewrite low-dwell /vs/* pages (target ones with high GSC impressions and <30s dwell)
- Case studies section on homepage (requires interviewing the 3 completers first)
